import 'package:flutter/material.dart';

class AddNovelScreen extends StatelessWidget {
  const AddNovelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('New Novel'),),
      body: Column(children: [
        
      ],),
    );
  }
}